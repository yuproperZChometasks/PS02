import requests

# Создаем словарь с данными для отправки
data = {
    'title': 'foo',
    'body': 'bar',
    'userId': 1
}

# Отправляем POST-запрос
response = requests.post('https://jsonplaceholder.typicode.com/posts', json=data)

# Распечатываем статус-код и содержимое ответа
print(f"Статус-код: {response.status_code}")
print("Ответ:", response.json())

"""
### Пояснение:
1. **Импорт библиотеки**: В начале мы импортируем библиотеку `requests`, которая позволяет работать с HTTP-запросами.
2. **Подготовка данных**: Создаем словарь `data`, который содержит данные для отправки.
3. **Отправка POST-запроса**: Используем метод `post()` для отправки данных на указанный URL. Параметр `json=data` автоматически сериализует словарь в формат JSON.
4. **Вывод результата**: Мы распечатываем статус-код ответа и содержимое ответа в формате JSON.
"""